version: "3.8"
services:
  api:
    build: ./server
    ports:
      - "3001:3001"
    volumes:
      - ./server/facturation.db:/app/facturation.db
      - ./server/.env:/app/.env
    environment:
      - NODE_ENV=production
